%h2
  Forgot your password?

- semantic_form_for resource_name, resource, :url => password_path(resource_name) do |form|
  = form.error_messages

  - form.inputs do 
    = form.input :email, :hint => "Send me reset password instructions"

  - form.buttons do
    = form.commit_button :button_html => { :value => "Reset" }  

%br
= render :partial => "shared/devise_links"
